# From Scratch Machine Learning Implementations in Python

This repository contains beginner-friendly implementations of core machine learning concepts built entirely from scratch using Python and NumPy — without using high-level machine learning frameworks such as TensorFlow or PyTorch.

The goal of this project is to understand the mathematical foundations and inner workings of machine learning algorithms by implementing them manually.

---

# Contents

1. Gradient Descent from Scratch
2. Artificial Neural Network (ANN) from Scratch

---

# 1. Gradient Descent from Scratch

## What is Gradient Descent?

Gradient Descent is an optimization algorithm used to minimize the loss (error) function in machine learning models.

It works by iteratively updating model parameters in the direction opposite to the gradient of the loss function.

### Intuition

Imagine standing on top of a hill and trying to reach the lowest point.

- The gradient tells you the direction of the steepest increase.
- Moving in the opposite direction helps you descend toward the minimum loss.

---  
# Artificial Neural Network (ANN) From Scratch in Python

This repository contains an implementation of an **Artificial Neural Network (ANN)** built completely from scratch using **Python** and **NumPy**, without relying on deep learning frameworks such as TensorFlow, Keras, or PyTorch.

The purpose of this project is to understand the internal working of neural networks, including forward propagation, backpropagation, activation functions, and gradient descent.

---

# What is an Artificial Neural Network?

An Artificial Neural Network (ANN) is a machine learning model inspired by the structure and functioning of the human brain.

It consists of interconnected nodes called **neurons** organized into layers:

- Input Layer
- Hidden Layer(s)
- Output Layer

Neural networks learn patterns from data by adjusting weights and biases during training.

---

# Neural Network Architecture

Typical architecture:

```text
Input Layer → Hidden Layer(s) → Output Layer
